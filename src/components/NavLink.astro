---
const { title, href, inDropdown } = Astro.props;
const isActive = Astro.url.pathname === href;
---

<li>
  <a href={href} 
     class={`hover:underline transition-all duration-200 ease-in-out ${isActive ? "underline" : "font-normal"} ${inDropdown ? "block w-full px-4 py-3 text-white hover:bg-zinc-700 rounded-md transition" : ""}`}>
    {title}
  </a>
</li>
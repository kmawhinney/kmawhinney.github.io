---
const { src, alt, caption, width = "full", height = "auto" } = Astro.props;
---

<figure class="flex flex-col items-center my-6">
  <img src={src} alt={alt} class={`rounded-lg shadow-lg w-${width} h-${height} object-cover`} loading="lazy"/>
  <figcaption class="text-sm text-zinc-400 mt-2 text-center">{caption}</figcaption>
</figure>
